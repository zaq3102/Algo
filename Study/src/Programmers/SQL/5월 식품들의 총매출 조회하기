-- 코드를 입력하세요

SELECT FP.PRODUCT_ID, FP.PRODUCT_NAME, (SUM(AMOUNT) * FP.PRICE) AS 'TOTAL_SALES'
FROM FOOD_PRODUCT FP, FOOD_ORDER FO
WHERE FP.PRODUCT_ID = FO.PRODUCT_ID AND 
    FO.PRODUCE_DATE >= '2022-05-01' AND FO.PRODUCE_DATE <= '2022-05-31'
GROUP BY FP.PRODUCT_ID
ORDER BY SUM(AMOUNT) * FP.PRICE DESC, FP.PRODUCT_ID;

#  SELECT절에 합곱연산 사용 가능!!(집계함수로 나온 결과끼리도 연산이 가능하다!)
#  ORDER BY절 역시 합곱연산 사용 가능!(집계함수도 사용 가능!)