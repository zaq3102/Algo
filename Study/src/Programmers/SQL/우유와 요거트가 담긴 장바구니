-- 코드를 입력하세요
SELECT DISTINCT CP.CART_ID
FROM CART_PRODUCTS CP, 
    (SELECT CART_ID FROM CART_PRODUCTS WHERE NAME = "Milk") AS MLK 
WHERE CP.CART_ID = MLK.CART_ID AND CP.NAME = "Yogurt"
ORDER BY CP.CART_ID;
# EASY SQL