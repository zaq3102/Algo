-- 코드를 입력하세요
SELECT R1.FOOD_TYPE, R1.REST_ID, R1.REST_NAME, R1.FAVORITES
FROM REST_INFO R1, (SELECT FOOD_TYPE, MAX(FAVORITES) AS R2V FROM REST_INFO GROUP BY FOOD_TYPE) R2
WHERE R1.FOOD_TYPE = R2.FOOD_TYPE AND R1.FAVORITES = R2.R2V
ORDER BY FOOD_TYPE DESC;

-- FROM절에 서브쿼리 사용시에는
-- 반드시 괄호 안에 넣어서 선언할 것!!
-- + GROUP BY 와 집계함수 사용 시 SELECT 내 속성값에 따라 결과값이 달라지므로 주의!